/**
 * Telegram 状态管理模块（已禁用）
 */

import { createSlice } from "@reduxjs/toolkit";

export const telegram = createSlice({
  name: "telegram",
  initialState: {},
  reducers: {},
});

export const {} = telegram.actions;
export default telegram.reducer;

export const FILE_SIZE = {
  // 默认文件大小限制为 2MB
  DEFAULT_MAX: 2 * 1024 * 1024,
  // 图片文件大小限制
  IMAGE_MAX: 2 * 1024 * 1024,
  // 视频文件大小限制 
  VIDEO_MAX: 10 * 1024 * 1024
}

// 文件大小提示文案
export const FILE_SIZE_MESSAGE = {
  IMAGE: '图片大小不能超过2MB',
  VIDEO: '视频大小不能超过10MB'
} 